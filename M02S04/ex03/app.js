const heroElement = document.querySelector('.hero');
const constrols = document.querySelector('.controls');

constrols.addEventListener('click', (event) => {
  const button = event.target;

  if (button.nodeName !== 'BUTTON') {
  }
});
